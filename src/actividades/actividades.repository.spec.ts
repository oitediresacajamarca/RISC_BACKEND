import { ActividadesRepository } from './actividades.repository';

describe('ActividadesRepository', () => {
  it('should be defined', () => {
    expect(new ActividadesRepository()).toBeDefined();
  });
});
