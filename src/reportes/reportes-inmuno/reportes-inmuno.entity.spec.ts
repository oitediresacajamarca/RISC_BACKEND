import { ReportesInmunoEntity } from './reportes-inmuno.entity';

describe('ReportesInmunoEntity', () => {
  it('should be defined', () => {
    expect(new ReportesInmunoEntity()).toBeDefined();
  });
});
