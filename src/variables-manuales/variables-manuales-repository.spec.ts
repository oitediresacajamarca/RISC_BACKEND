import { VariablesManualesRepository } from './variables-manuales-repository';

describe('VariablesManualesRepository', () => {
  it('should be defined', () => {
    expect(new VariablesManualesRepository()).toBeDefined();
  });
});
